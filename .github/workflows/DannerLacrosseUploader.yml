name: Danner Lacrosse Uploader
on:
  workflow_dispatch:

jobs:
  run_automate:
    runs-on: ubuntu-latest
    steps:
      - name: Run automate.yml
        uses: actions/checkout@v2
      - name: Run automate.yml
        uses: ./.github/workflows/automate.yml
        # Add any additional setup or environment variables needed

  run_excel_csv:
    needs: run_automate
    runs-on: ubuntu-latest
    steps:
      - name: Run excel_csv.yml
        uses: actions/checkout@v2
      - name: Run excel_csv.yml
        uses: ./.github/workflows/excel_csv.yml
        # Add any additional setup or environment variables needed

  run_flxpoint:
    needs: run_excel_csv
    runs-on: ubuntu-latest
    steps:
      - name: Run flxpoint.yml
        uses: actions/checkout@v2
      - name: Run flxpoint.yml
        uses: ./.github/workflows/flxpoint.yml
        # Add any additional setup or environment variables needed
