name: Danner Lacrosse Uploader

on:
  workflow_dispatch:

jobs:
  automate:
    name: Automate Job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run automate.yml
        run: |
          # Run automate.yml script or commands here

  excel_csv:
    name: Excel CSV Job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run excel_csv.yml
        run: |
          # Run excel_csv.yml script or commands here

  flxpoint:
    name: Flxpoint Job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run flxpoint.yml
        run: |
          # Run flxpoint.yml script or commands here
